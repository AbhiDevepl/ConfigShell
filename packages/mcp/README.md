# `@configshell/mcp`

**Placeholder. Nothing is implemented.** This package exists to reserve the home of the
MCP (Model Context Protocol) layer; it has no source files, no entry point, and no
dependencies.

MCP is intended to expose controlled, explicitly authorized capabilities to AI systems —
never raw shell access. See [`docs/mcp.md`](../../docs/mcp.md) and
[`docs/security-model.md`](../../docs/security-model.md).

This is post-V1 work (see [`ROADMAP.md`](../../docs/ROADMAP.md)). Open an issue before starting
on it.
