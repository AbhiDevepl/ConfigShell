# `@linux-app-platform/ai`

**Placeholder. Nothing is implemented.** This package exists to reserve the home of the
AI planning layer; it has no source files, no entry point, and no dependencies.

The design constraints any implementation must satisfy — AI plans and recommends, it never
executes, and every planned operation flows through the same validation and confirmation
path as a manual one — are in [`docs/ai.md`](../../docs/ai.md) and
[`docs/security.md`](../../docs/security.md).

This is post-V1 work (see [`ROADMAP.md`](../../ROADMAP.md)). Open an issue before starting
on it.
