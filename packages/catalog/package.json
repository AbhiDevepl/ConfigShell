{
  "name": "@configshell/catalog",
  "version": "0.1.0",
  "private": true,
  "description": "Shared, verified application catalog — the single source of truth for application metadata",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/*.test.ts"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^26.5.1",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2"
  }
}
