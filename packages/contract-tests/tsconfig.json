{
  "// why": [
    "Contract tests import `server/app` directly -- there are no declaration",
    "files for a build-free workspace -- so this project's program includes the",
    "server source, which must therefore typecheck under strict settings too.",
    "No `allowJs`: the server graph it imports is TypeScript now."
  ],
  "extends": "../../tsconfig.base.json",
  "include": ["src"]
}
