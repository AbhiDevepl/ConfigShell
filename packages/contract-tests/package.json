{
  "name": "@configshell/contract-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-adapter contract tests: the HTTP API and the MCP server must answer the same question the same way. Test-only; ships nothing.",
  "type": "module",
  "scripts": {
    "test": "tsx --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@configshell/catalog": "*",
    "@configshell/mcp": "*",
    "@types/node": "^22.14.0",
    "server": "*",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2"
  }
}
