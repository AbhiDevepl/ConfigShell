{
  "name": "configshell",
  "version": "0.1.0",
  "private": true,
  "description": "ConfigShell \u2014 open-source Linux software discovery and setup-plan generation",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@12.3.4",
  "scripts": {
    "dev": "pnpm --parallel --filter web --filter server dev",
    "build": "npm run build --workspace=web",
    "start": "npm run start --workspace=server",
    "lint": "eslint .",
    "typecheck": "npm run typecheck --workspace=web && npm run typecheck --workspace=@configshell/catalog && npm run typecheck --workspace=@configshell/installer && npm run typecheck --workspace=@configshell/test-utils && npm run typecheck --workspace=@configshell/mcp && npm run typecheck --workspace=server && npm run typecheck --workspace=@configshell/contract-tests",
    "test": "npm run test --workspace=@configshell/test-utils && npm run test --workspace=@configshell/catalog && npm run test --workspace=@configshell/installer && npm run test --workspace=@configshell/mcp && npm run test --workspace=server && npm run test --workspace=@configshell/contract-tests && npm run test --workspace=web",
    "check": "npm run lint && npm run typecheck && npm run test && npm run build",
    "dev:web": "npm run dev --workspace=web",
    "mcp": "npm run start --workspace=@configshell/mcp"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.12.0",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2",
    "typescript-eslint": "^8.46.0"
  }
}
