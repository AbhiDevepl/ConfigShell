/**
 * Public entry point for server configuration.
 *
 * Import configuration from here (`./config/index.js`) rather than reaching
 * into individual config modules, so the set of configuration sources stays
 * visible in one place as the server grows.
 */

export { env } from "./env.js";
