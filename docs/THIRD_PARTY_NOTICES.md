# Third-party notices

ConfigShell is distributed under the [Apache License 2.0](../LICENSE). It uses the
third-party software listed below, each under its own license. Nothing here is vendored
into the repository except where explicitly stated — dependencies are installed from npm
via `pnpm-lock.yaml`.

License identifiers below were read from each package's own `package.json` at the time of
writing. Versions are intentionally not listed (they change with every lockfile update);
the authoritative record is `pnpm-lock.yaml`. **If you add or remove a dependency, update
this file in the same pull request.**

## Bundled into the production build

These end up in `apps/web/dist` and are therefore distributed to users of a deployed build.

| Package | License | Notes |
| ------- | ------- | ----- |
| `react`, `react-dom` | MIT | Meta Platforms, Inc. and affiliates |
| `radix-ui` | MIT | WorkOS — unstyled UI primitives |
| `lucide-react` | ISC | Icon set; a fork of Feather Icons (MIT) |
| `class-variance-authority` | Apache-2.0 | |
| `cn` | MIT | class-name helper used by the shadcn components |
| `tailwindcss`, `@tailwindcss/vite` | MIT | CSS is generated at build time |
| `tw-animate-css` | MIT | animation utilities imported by `src/index.css` |
| `shadcn` | MIT | `shadcn/tailwind.css` is imported by `src/index.css` |
| `@fontsource-variable/geist` | **OFL-1.1** | see "Fonts" below |
| `express` | MIT | used by `apps/web/server.js` to serve the built app |
| `@modelcontextprotocol/server` | MIT | the official MCP TypeScript SDK (v2), used by `packages/mcp` |
| `@modelcontextprotocol/core` | MIT | shared protocol types and schemas, pulled in by the server package |
| `zod` | MIT | tool input schemas in `packages/mcp`; the SDK derives JSON Schema from them |

## Build and development tooling

Not shipped to users, but part of the repository's toolchain.

| Package | License |
| ------- | ------- |
| `vite`, `@vitejs/plugin-react` | MIT |
| `typescript` | Apache-2.0 |
| `eslint`, `@eslint/js` | MIT |
| `typescript-eslint` | MIT |
| `eslint-plugin-react-hooks` | MIT |
| `globals` | MIT |
| `tsx` | MIT |
| `@modelcontextprotocol/client` | MIT |
| `dotenv` | BSD-2-Clause |
| `@types/node`, `@types/react`, `@types/react-dom`, `@types/express` | MIT (DefinitelyTyped) |

`express` is also a runtime dependency of `apps/server` (MIT).

`@modelcontextprotocol/client` is a development dependency only: it is the official MCP
client, used by `packages/mcp`'s interoperability tests to connect to our own server over the
real protocol. It is never shipped.

`tsx` is listed here as tooling but is also how `apps/server` **runs**: the server is
JavaScript that imports the workspace's TypeScript packages directly, and `tsx` transpiles
them on the fly so the monorepo needs no build step. `nodemon` was removed in the same
change — `tsx watch` covers what it did.

## Fonts

**Geist Variable**, via `@fontsource-variable/geist`, is licensed under the
[SIL Open Font License 1.1](https://openfontlicense.org) — Copyright 2024 The Geist Project
Authors (https://github.com/vercel/geist-font).

The OFL requires that the font's copyright and license notice accompany any distribution of
the font files. The `.woff2` files are emitted into `apps/web/dist/assets/` by the build, so
**any deployment of this project distributes them**, and this notice must travel with it.
The full license text ships inside the package at
`node_modules/@fontsource-variable/geist/LICENSE`.

## Icons

**Lucide** (`lucide-react`) is ISC-licensed and is itself a fork of
[Feather Icons](https://github.com/feathericons/feather) (MIT, Copyright 2013–2023 Cole
Bemis). Both licenses require their copyright and permission notice to be preserved; the
notices ship inside the installed package.

No other icon sets, illustrations, or images are used. `apps/web/public/` contains no
images: two unreferenced scaffold images of unclear provenance were removed rather than
published (they remain in the git history — see `SECURITY.md` on why deleting a file does
not remove it from history).

## Code derived from other projects

- **shadcn/ui** — the components in `apps/web/src/components/ui/` were generated by the
  shadcn CLI and are derived from [shadcn/ui](https://github.com/shadcn-ui/ui), which is
  MIT-licensed (Copyright 2023 shadcn). shadcn/ui is explicitly distributed as code you
  copy into your project; the files have been kept close to their generated form, and the
  MIT license permits their use and modification here.
- **Vite AI Studio scaffold** — `apps/web/vite.config.ts` contains a small media-serving
  dev plugin (`aistudioMediaPlugin`) that originated in a Google AI Studio project
  template, and `apps/web/metadata.json` is an AI Studio manifest. Both are development
  scaffolding, not shipped code.

No other third-party source code has been copied into this repository.

## Application names in the catalog

`packages/catalog` records the names, descriptions, homepages, and package identifiers of
third-party applications (Firefox, VS Code, Docker, …). Those names and logos are the
trademarks of their respective owners, and referring to them here is nominative use:
**this project is not affiliated with, endorsed by, or sponsored by any of them**, and it
redistributes none of their software. The catalog carries no application binaries, no
installer payloads, and no logo assets.

## Verifying this yourself

```sh
pnpm licenses list            # licenses of the full installed dependency tree
pnpm why <package>            # why a given package is installed
```

`pnpm licenses list` covers transitive dependencies too; this file lists only the direct
ones, which is what a reviewer usually needs.

At the time of writing, the full installed tree contains no GPL-, LGPL-, or AGPL-licensed
packages. The only non-permissive licenses present are **OFL-1.1** (the Geist font, covered
above) and **MPL-2.0** (`lightningcss`, pulled in by Tailwind as a build tool — file-level
copyleft that imposes no obligation on this project's own source, since the package is used
unmodified and is not redistributed).
